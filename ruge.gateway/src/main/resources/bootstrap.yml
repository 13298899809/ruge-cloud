spring:
  application:
    name: ruge-api-gateway
  # 本地调试使用
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true  #开启根据微服务名称自动转发
          lower-case-service-id: true #微服务名称以小写形式呈现

    consul:
      host: 172.20.2.126
      port: 8500
